<%= form_for(@rating) do |f| %>
  <% if @rating.errors.any? %>
    <div id='error_explanation'>
      <h3><%= pluralize(@rating.errors.count, 'error') %> prohibited this rating from being saved:</h3>

      <ul>
      <% @rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field'>
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>
